<h3>Create an Item for <%= @restaurant.name %> </h3>
<%= render partial: 'form', locals: {item: @item} %>

<div id="itemResult">
</div>
<br>
<%= link_to "Back to Restaurant", restaurant_path(@restaurant) %> | <%= link_to "Create Another Item", new_restaurant_item_path(@restaurant) %> | <%= link_to "Back to All Restaurants", restaurants_path %>
